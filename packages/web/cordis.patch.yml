# Web-only companion: the canonical WorkSurface service remains usable in
# CLI/headless profiles without webServer or sessions.
- insert:
    - id: pf-worksurface-web
      name: "@pf-worksurface/web"
      config:
        trustedHosts: []
