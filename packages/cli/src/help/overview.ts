export function overviewHelp(version: string): string {
  return `ws ${version}\n\nWorkSurface keeps durable, independently assessable work in Surfaces and coordinates existing Surfaces when that structure helps. Use an ordinary DSH Session for work that does not benefit from this separation.\n\nUsage:\n  ws help [author|coordinate|emit|recover]\n  ws emit <event-name> [--surface <surface-id>] [--key <operation-key>]\n     [--payload <json> | --payload-file <path>]\n\nChoose the current model-owned action:\n  author       Maintain the current Surface or author another Surface.\n  coordinate  Relate existing Surfaces with ordinary Orchestrate code.\n  emit         Submit an output authorized by the current Turn Brief.\n  recover      Reconcile authoring after an interrupted or uncertain Turn.\n\nRead the relevant help before authoring. Runtime-owned identity, persistence, concurrency, and recovery internals are not model inputs.\n`
}
