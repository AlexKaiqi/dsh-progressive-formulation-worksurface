export const RECOVER_HELP = `WORKSURFACE RECOVERY\n\nPURPOSE\nContinue model-owned authoring after a Turn was interrupted or an external effect has an uncertain outcome.\n\nUSE WHEN\nThe Session reports restart continuation, interruption, conflict, or an uncertain tool result.\n\nDO\n1. Run ws recover to retry unfinished managed operations using existing durable facts, then ws list to inspect the existing Surfaces. Read the complete recover response, including runtime.failedRegistrations and each lastFailure; counts alone do not identify what needs repair. Preserve Surface ids and the original run request keys.\n2. For a revision-conflict, read lastFailure.message to locate the affected Surface and authoring directory. Inspect that Surface's preserved drafts before choosing how to reconcile them. A downstream Surface can be blocked even when the upstream publication succeeded.\n3. If the preserved changes should become the confirmed files, request reconciliation of that existing Surface with ws run when it has no active Turn, then use ws publish from its active Surface Turn. Otherwise reconcile the files deliberately while preserving useful drafts. Do not assume re-emitting an upstream Event or using a new upstream run key resolves a downstream file conflict.\n4. After resolving the reported conflict, run ws recover again to retry the already accepted input. Inside a Surface Turn, re-read WORKSURFACE_SURFACE_DIR and the current WORKSURFACE_VIEW_DIR/turn-brief.json.\n5. Inspect durable files and observable external results before repeating work. Reuse a publication key only to retry that same publication; new file snapshots need new keys. A prior business Event alone does not prove its supporting files were published.\n6. Do not blindly repeat a non-idempotent external side effect. Continue only the model-owned repair requested by the current error or Brief; Runtime performs its own deterministic recovery.\n\nNEXT\nAfter reconciling the authoring state, use ws help author, ws help coordinate, ws help publish, or ws help emit for the next actual action.\n`
