export const AUTHOR_HELP = `WORKSURFACE AUTHORING\n\nPURPOSE\nMaintain one Surface as the independent long-term reasoning context of one line of work, or author another Surface when a part is independently solvable and independently verifiable or a workstream must evolve its own durable context independently.\n\nUSE WHEN\n- starting a first Surface from an ordinary Agent Session;\n- continuing the Surface bound to the current Session; or\n- preparing additional Surfaces before coordinating them.\n\nPRECONDITIONS\n- WORKSURFACE_ROOT locates the public authoring root in model shell calls.\n- Inside a Surface Turn, WORKSURFACE_SURFACE_DIR is the current Surface and WORKSURFACE_VIEW_DIR/turn-brief.json is the current instruction and output authority.\n\nDO\n1. Choose a Surface id matching [A-Za-z0-9][A-Za-z0-9._-]{0,127}.\n2. Create WORKSURFACE_ROOT/surfaces/<surface-id> with ordinary file or script capabilities.\n3. Create surface.md with these seven headings in order:\n   # Goal\n   # Acceptance Criteria\n   # Known Facts and Constraints\n   # Assumptions\n   # Open Questions\n   # Current Decisions\n   # Deliverables and Evidence\n4. Keep that Surface's code, materials, fixtures, deliverables, and evidence in the same directory. Do not add Runtime-owned identity or status frontmatter.\n5. Run ws sync to validate and admit the authored materials, then ws list to verify the Surface was admitted. A validation error must be corrected before proceeding.\n6. Run ws run <surface-id> --instruction <business-task> --key <stable-request-id> to ask the host to start or continue that Surface. Keep the same key when retrying an uncertain request; use a new key for a new instruction. This does not switch the calling session to another Surface.\n7. Inside the active Surface Turn, read filePublication in turn-brief.json. After writing and checking files, run ws publish --key <stable-publication-key> [--summary <text>] to confirm them for downstream readers. Keep the same key for an uncertain retry; use a new key for newly changed files. Publish before emitting a business Event that depends on those files, and publish final deliverables even when outputs is empty. File publication does not imply business acceptance.\n\nFORK\nA fork needs no special creation method: copy the Surface directory, replace the divergent parts, then run ws sync. Record the inheritance under # Known Facts and Constraints, for example "forked from <surface-id>, replaced: <what changed>". Keep a fork bounded: its result folds back to readers through published files, and a copy drifts as the original evolves. Exploration that needs no durable copy of its own does not need a Surface.\n\nVALIDATE\nThe Surface must have a concrete goal, independently checkable acceptance criteria, and all seven ordered headings. Before emitting an output, continue with: ws help emit\n`
