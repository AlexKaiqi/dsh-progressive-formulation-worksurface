export const VERSION = '0.2.0-rc.1'

export const HELP = `ws ${VERSION}\n\nPlan and advance file-native WorkSurfaces through the WorkSurface Host.\n\nUsage:\n  ws emit <event-name> [--surface <surface-id>] [--key <operation-key>]\n     [--payload <json> | --payload-file <path>]\n\n  ws surface create <surface-id> --contract-file <surface.md>\n\n  ws orchestrate register <orchestration-id> --definition-file <definition.json>\n     (--bindings <json> | --bindings-file <path>) --registration <registration-id>\n\nRules:\n  A Surface Session is bound before its first Turn; the Agent cannot open or switch Surfaces for the current Session.\n  A live Turn may create independent Surface contracts and register exact orchestration Definitions.\n  Register the orchestration before emitting root facts so dependency-free branches cannot miss activation.\n  DSH_SURFACE_ID, DSH_SURFACE_DIR, and DSH_CONTEXT_FILE describe the fixed current Surface.\n  ws emit uses the bound Surface inside a Turn; outside a Turn, --surface and --socket are required.\n  surface.revision.published snapshots the Surface Session cwd and publishes it with compare-and-swap.\n  DSH waiting, failure, cancellation, retry, and completion are not WorkSurface events.\n`
