# WorkSurface event/revision service, DSH Session integration, and orchestration reconcile.

- insert:
    - id: pf-worksurface
      name: '@pf-worksurface/dsh'
      config:
        root: !!js dshHomePath('pf-worksurface')
