version: 1
roles: [coordinator, researcher]
patterns:
  delegate-research:
    kind: delegate
    history: from-registration
    key: $.payload.taskId
    when:
      from: coordinator
      event: research.requested
    to: researcher
    message: Research task ${activation.key}
    operation: start-research
