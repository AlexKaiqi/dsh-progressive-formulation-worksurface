version: 1
roles: [worker]
patterns:
  retry-until-done:
    kind: loop
    history: from-registration
    key: $.payload.taskId
    when:
      from: worker
      event: task.started
    until:
      from: worker
      event: task.completed
